# Hex GPU Cloud credentials
HEX_USER=your_username
HEX_NODE=your_node.cs.bath.ac.uk

# Remote working directory (on /mnt/fast0, not home — home has a 25GB quota)
HEX_WORK_DIR=/mnt/fast0/${HEX_USER}/gsplat_work

# Local paths
LOCAL_RESULTS_DIR=./output
